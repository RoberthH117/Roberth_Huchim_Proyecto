version: '3.4'

services:
  backend:
    build:
      context: .
      dockerfile: QualfixAdmin/Dockerfile
    ports:
      - "5000:80"
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
    networks:
      - app-network
